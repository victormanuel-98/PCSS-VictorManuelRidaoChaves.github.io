## Notes
Proyecto visual 100% frontend (sin backend).
Los proyectos se cargan desde datos locales en `src/main.js`.
El formulario de contacto es solo UI.
La textura local `/images/profile.jpeg` se usa en About.
